[pytest]
testpaths = tests/
addopts = -n 2 --reruns 2 --reruns-delay 2
markers =
    smoke: Critical path tests - run on every deploy
    regression: Full coverage tests - run nightly or pre-release
    api: API-level tests
